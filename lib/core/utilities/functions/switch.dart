import 'package:flutter/foundation.dart';

class ProjectSwitch {
  static const bool prints = kDebugMode;
  static const bool deviceInfoPrints = kDebugMode;
  static const bool buildPrints = kDebugMode;
  static const bool networkPrints = kDebugMode;
}
